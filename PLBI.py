"""
fichier contenant la résolution d'un programme linéaire sur
un MDP bi critère en politique mixte
"""

from Env import EnvLabyrinthe
import numpy as np
import time
from tqdm import tqdm
import matplotlib.pyplot as plt


def resolution(
    nb_ligne : int,
    nb_colonne : int,
    
) :
    pass